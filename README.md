
  # DATprepplus (Community)

  This is a code bundle for DATprepplus (Community). The original project is available at https://www.figma.com/design/waRNwPUghSTIwIHBt2hZv7/DATprepplus--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  